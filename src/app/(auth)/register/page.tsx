import { AuthForm } from "@/components/AuthForm";

export const metadata = { title: "Create account — Movie Match" };

export default function RegisterPage() {
  return <AuthForm mode="register" />;
}
